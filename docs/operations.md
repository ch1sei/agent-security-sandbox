# Operations Notes

The security console is designed around a decision stream: request, matched policy, risk, action, and audit evidence. Review actions remain explicit and should never be inferred from a dashboard color alone.

The current console is a static product preview with placeholder policy contracts.
